# urdf-to-mjcf — runtime requirements
#
# Prefer K-Scale Labs `urdf2mjcf` for URDF -> MJCF conversion.  The
# bundled wrapper can fall back to MuJoCo's built-in URDF loader, but
# that raw output usually needs more manual post-tuning.

mujoco>=3.5
lxml>=4.9
urdf2mjcf>=0.2.39
xacro>=2.0

# Runtime deps used by the vendored urdf2mjcf copy in pathonai_diy_pipeline.
colorlogging
pydantic
trimesh
